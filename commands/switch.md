---
description: Switch between Claude Code accounts
allowed-tools: Bash(~/ccswitch.sh:*)
---

# Switch Login

Switch between configured Claude Code accounts using the multi-account switcher.

Run the account switcher:

```bash
~/ccswitch.sh --switch
```

Execute this command immediately to display available accounts and allow switching to a different Claude Code login.
